# Taste
- Prefers professional, minimalist frontend/UI design — clean aesthetic with theme/background that ties into the project concept. Confidence: 0.8
- Prefers "Zero-Database" architecture (no traditional database; data handled via static/files-based structure) when feasible. Confidence: 0.8
- Prefers an explicit "Implementation Planning & Workflow Management" phase before moving to production. Confidence: 0.9
- Prefers communicating in Indonesian (Bahasa Indonesia). Confidence: 0.8
- Prefers approved implementation plans & workflow to be saved as a persistent document inside the project root (e.g., IMPLEMENTATION_PLAN.md), not only in the global plans directory, so other agents/team members can stay in sync. Confidence: 0.8
- Prefers the agent to use relevant built-in or global skills (e.g., plan, design, research skills in the .agents folder) whenever available for a task. Confidence: 0.8
- Prefers implementation plan and workflow to be kept as two separate documents in the project root (technical blueprint in IMPLEMENTATION_PLAN.md, execution process/phase/commit rules in WORKFLOW.md), cross-referencing each other, rather than one combined doc. Confidence: 0.8
- Prefers website/catalog content (repo descriptions, UI copy) in English — always good and formal English — while conversation with the user stays in Indonesian. Confidence: 0.9
- When adding a repo to the catalog, wants a substantial professional description (at least ~10 sentences) plus concise key-point highlights that distill only the essential parts/outline. Confidence: 0.9
- New catalog entries must keep the exact same structure and components as existing entries (no structural changes when adding content); existing entries serve as the reference template. Confidence: 1.0
- Prefers using real images that already exist inside the source repository (logo, screenshot) over placeholder fallbacks when adding repos; willing to provide a GitHub token for that. Confidence: 0.8
- Commits/pushes go only to the designated working sub-branch (e.g., commandcode, tracking origin/preview); never commit/push to preview/master without explicit user instruction. The agent must also stay/operate on that sub-branch, not on other branches (e.g., - Commits/pushes go only to the designated working sub-branch (e.g., commandcode, tracking origin/preview); never commit/push to preview/master without explicit user instruction. The agent must also stay/operate on that sub-branch, not on other branches (e.g., user explicitly warned not to stay on `cline` origin; later explicitly instructed "commit push ke commandcode origin"). Confidence: 1.0
- Never delete, modify, or move any git branch other than the agent's own working branch — other branches (e.g., `cline`) may be another agent's workspace and must be left untouched; any git operation that touches another branch requires explicit user confirmation first. Confidence: 1.0
- Before starting work on the dedicated working sub-branch, update/sync it from its upstream first (e.g., `git fetch origin preview` + merge) — the user explicitly asked "update sub branch mu dulu dari preview" before proceeding with the task, and later issued the raw command `git checkout commandcode origin` to ensure the working branch is on `commandcode` and in sync with origin. Confidence: 0.8
- Expects the agent to verify that a push actually landed on the intended remote branch (e.g., check `git branch -vv` / `git log origin/<branch>` after pushing) — pushes can silently go to the wrong branch due to branch-name/environment quirks, and the user will flag it if changes aren't on the expected branch. Confidence: 0.7
- Push destination is context-dependent: some projects use direct `main` pushes (git push origin main), while others use a dedicated working sub-branch; the "always push to main" preference does not generalize. Confidence: 0.4
- When existing catalog categories do not cover a repo's content, prefers keeping the catalog schema/type unchanged and mapping the repo to the closest existing category (e.g., security/privacy repos → developer-tools/backend/other-fun; self-hosted → communication) rather than extending the CategoryId type — user explicitly said "struktur dan komponen katalog masih sama tidak ada yang berubah, kamu hanya tambah link repo baru + informasinya" and "Gunakan struktur dan komponen katalog yang sudah ada di website sebagai contoh biar kamu ga salah". Confidence: 0.8
- For long multi-part implementation tasks, prefers completing one category at a time starting from the earliest, with review and verification before moving on. Confidence: 0.8
- Prefers reviewing code changes and checking for errors/safety (lint, build, validation) after each step, and only committing/pushing when the review passes — user expects the work to follow the established workflow ("sesuai alur kerja") without deviating. Confidence: 0.8
- When working in a shared repo where other agents also operate, stages and commits only the files belonging to the current task (explicit `git add` of own files); leaves other agents' uncommitted files and temp scripts untracked and untouched, never `git add -A`. Confidence: 1.0
- Uses conventional commit messages for catalog work: `feat: add N hidden gem repos to <Category> catalog` subject, a body summarizing what was added, and a `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer — written to a scratchpad file and committed via `git commit -F` (Windows cmd lacks heredoc support). Confidence: 0.85
- When implementing catalog entries from a research document (e.g., Bahan.md, hidden_gems_repos.md), reads the source document first and treats it as the authoritative list of repos to add, following its numbering. Confidence: 1.0
- Verifies every repo's existence and metadata against the GitHub API (credible source) before adding it to the catalog, instead of trusting the research document blindly — user asked to "gunakan sumber informasi yang kredibel" (use credible information sources). Confidence: 0.85
- Does not duplicate repos that other agents have already added: before writing new entries, checks the existing catalog (slug/fullName) and skips any repo already present, even if it's on the task's source list. Confidence: 0.9
- Expects the catalog to be kept complete against the source document: when asked to add repos from a source list (e.g., "TOP 10 PICKS BARU (Batch 2)" or "kategori J"), the agent must first diff every repo link in the document against what's already in the catalog and add the missing ones — not just what's convenient; a later task explicitly asked to "cari link repo mana yang belum terimplementasi di website project. Setelah itu anda bisa masukkan seluruh link repo tersebut secara terstruktur dan lengkap." and then, when told nothing was missing, followed up with "anda tambahkan repo yang belum ada ke website project" — i.e., the user insists every repo mentioned in the source (even ones the doc flagged as "excluded") gets an entry. Confidence: 0.9
- When the user repeats/insists a task ("anda tambahkan repo yang belum ada ke website project") after the agent reported no work was needed, the user wants the agent to dig deeper and find the actual gap — not just re-state the earlier conclusion; the agent should look for repos mentioned in the source without full URLs and treat those as candidates to add. Confidence: 0.85
- New repo entries should be verified against the GitHub API for existence and live metadata (stars, license, language, topics, dates) before being added, and that live data should also be merged into the site's generated stats file — user expects freshly added repos to have real, current stats, not placeholders. Confidence: 0.8
- After adding repos, expects the full verification chain before reporting done: TypeScript type check (`tsc --noEmit`) passes, production build succeeds, and the generated static pages for the new repos are confirmed to exist in the build output. Confidence: 0.85
- Is allowed to delegate exploration/verification subtasks to subagents for efficiency when working on large catalog additions. Confidence: 0.8
- Prefers isolating each agent's workspace in a dedicated git worktree on its own sub-branch tracking `origin/preview`, rather than multiple agents sharing one working directory — avoids branch-switching collisions between concurrent agents (explicitly requested "buat worktree baru dengan nama commandcode & set sub branch worktree ke commandcode/origin/preview"). The worktree is kept inside the project directory as a hidden dot-folder (e.g., `.commandcode worktree`) rather than as a sibling folder outside the repo (user renamed and moved it into the project). Confidence: 0.75
- Prefers local workspace/config folders inside a project (e.g., `/.commandcode`, `/.commandcode worktree`) to be added to `.gitignore` so the repo's git status stays clean of agent workspace folders; when moving a worktree to a new location, expects the agent to run `git worktree repair` and verify git still tracks it correctly. Confidence: 0.7
- When the user gives an explicit instruction (e.g., "push ke origin/commandcode" or "push to origin branch first"), execute it directly rather than pausing to re-ask or second-guess the target — the user got visibly frustrated ("itu punya mu dongo") when the agent questioned an already-explicit push destination. Confidence: 0.85
e-authorizes the push upfront (e.g., "kalo sudah kamu tinggal push aja ke sub branch commandcode origin") and expects the agent to complete the work through the push without pausing for approval. Confidence: 0.95
- Before pushing, wants the agent to first check/list all configured origin remotes (e.g., `git remote -v` on the repo and any worktree) to confirm the correct push target — user explicitly instructed "cek dulu semua remote origin yg ada" before allowing the push, rather than pushing to an assumed remote. Confidence: 0.6
- Treats `main` as the canonical branch and wants all sub-branches (e.g., commandcode, preview, cline) — both local and origin — synced to it when asked to "sync all sub branch origin and worktree from main": fetch with prune, then fast-forward each branch from main and push it to origin (plain `--ff-only` merge, no force push), return to `main`, and verify every branch sits at the same commit. Confidence: 0.65
